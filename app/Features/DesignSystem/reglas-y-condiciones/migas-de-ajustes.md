# Migas de ajustes

Registro vivo de hallazgos derivados de auditorías del Design System. Usa esta lista para priorizar y dar seguimiento a pequeños trabajos de mejora.

## Pendientes
- [ ] Migrar formularios de `categorías` (`create`, `edit`) a componentes del Design System (`x-input-with-label`, `x-textarea-with-label`, `x-select-basic`, `x-switch-basic`).
- [ ] Migrar formularios de `variables` (`create`, `edit`) a componentes del Design System con lógica Alpine preservada.

## Ajustes resueltos
- [x] Unificar la paleta de `x-badge-soft` en listados y detalles de categorías, variables, cupones y sliders (08-11-2025).
